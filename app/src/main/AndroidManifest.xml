<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventoryappariellemoore">

    <!-- Runtime permission is still required inside code -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Prevents “Permission exists without corresponding hardware” warning -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryAppArielleMoore">

        <!-- Launcher/Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"/>

        <!-- Item Management Activity -->
        <activity
            android:name=".ItemManagementActivity"
            android:exported="false"/>

        <!-- SMS Permission Activity -->
        <activity
            android:name=".SmsPermissionActivity"
            android:exported="false"/>

        <!-- Register Activity -->
        <activity android:name=".RegisterActivity"/>

    </application>
</manifest>
